# Security and architecture audit, September 2026

Audited state: `dfe9e59` on `main`, 2026-09-05. Statuses are current as of
2026-09-07 and name the pull request that closed each item, so this file can be
read against `git log` rather than trusted. The dependency scan was re-run on
2026-09-11 against the PR #23 lockfile; that snapshot is recorded in
[Dependencies](#dependencies).

One of six repositories audited in the same pass. This one has the smallest
attack surface (one public function, everything else client-side), and one
finding that matters because the function spends money.

## Method

One read-only reviewer per repository, in parallel, no paid or external calls.
This repository's reviewer broke `historyStore` with six corrupted documents
rather than reasoning about which shapes it would tolerate. On top of that,
one deterministic check across all six: no `.env` tracked at any point in
history, `.gitignore` covering it, zero secret patterns, zero
`pull_request_target`.

Every HIGH finding was verified in the code before it was written down.

## Summary

| Severity | Count | Theme |
|---|---|---|
| Critical | 0 | |
| High | 1 | cost without a ceiling on the paid endpoint |
| Medium | 4 | |
| Low | 4 | |

## Patterns shared with the other five repositories

1. **CI hygiene.** `permissions: contents: read` landed in PR #22. This
   repository uses only first-party `actions/*`, so nothing to SHA-pin.
2. **Documentation asserting what the code did not do.** `README.md` promised
   resilience to corrupted stored data that `historyStore` did not deliver.
   Fixed in the code rather than the README (PR #22).

## High

| # | Finding | Where | Fix | Status |
|---|---|---|---|---|
| H1 | `/api/coach` has no rate limit. The per-field caps bound the cost of one request, not the number of requests. A caller without an `Origin` header (curl) passes every gate, and each POST is a paid call to the model. | `api/coach.ts` | A per-IP limit (Vercel Firewall rules, or `@upstash/ratelimit` backed by Vercel KV) plus a daily ceiling on total calls: **limit reached → 429 with no model call**; only a failure of the limiter/storage control fails closed to the existing 503 path the UI already handles. | **Open.** Planned as Phase 11 in `ROADMAP.md`. |

## Medium and low

- [x] `historyStore.migrateHistory` validated arrays but not their elements; three corrupted documents froze the landing page. Element-level sanitisers mirroring `sanitizeProfile`; 7 tests, all red without the fix. PR #22.
- [x] `ErrorBoundary` wrapped only `/training`; `/`, `/progress` and `/profiles` showed a blank page on a render error. Boundary on every route. PR #22.
- [x] `err.message` from the Anthropic SDK was returned to the anonymous caller, which distinguishes an invalid key from a nonexistent model: free reconnaissance. Now `{ error: 'coaching_failed' }` only. PR #22.
- [ ] `.github/instructions/*.instructions.md` has a literal `*` in the file name; `git clone` fails on Windows.
- [ ] `npm audit fix` without `--force` closes advisory GHSA-qwww-vcr4-c8h2 (`react-router`, RSC-only, not reachable here, but it removes audit noise). Do not run `--force`: it proposes a two-major downgrade of `@vercel/node`.
- [ ] `selectors.ts` and `BoxingEngine.ts` have no tests; `gamification/index.ts` is a dead barrel.
- [ ] `vercel.json` sets `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options` and `Permissions-Policy`, but no `Strict-Transport-Security` and no `Content-Security-Policy`. Add HSTS; add CSP as `Report-Only` first (the MediaPipe WASM from jsDelivr and the model from Google Cloud Storage need explicit sources).

## Dependencies

Snapshot of 2026-09-11, re-run for PR #23 at commit `3b7a036` (Node v22.14.0,
npm 10.9.2). No dependency changed in this PR. Counts below are a point-in-time
reading: `npm audit` classifies against the advisory database as it exists on
the day of the run, so a later snapshot may report different severities or
counts with the same lockfile.

The shape of the chain, from `npm ls react-router-dom react-router`:

    react-router-dom@7.18.1   direct dependency (package.json)
    └── react-router@7.18.1   transitive (a dependency of react-router-dom)

`npm audit --omit=dev` and `npm audit --omit=dev --audit-level=high` both exit
1 and report **2 high-severity vulnerabilities**: `react-router` 7.12.0–7.18.1
(advisory GHSA-qwww-vcr4-c8h2, RSC Mode CSRF bypass, RSC-only and not reachable
here) and `react-router-dom` (npm reports it as "depends on vulnerable versions
of react-router"). That is **one advisory reported on two packages** (the
vulnerable one plus the direct dependency that pulls it in), not two distinct
vulnerabilities. This PR does not change the lockfile, so the finding stays
open.

The full `npm audit` (dev and build included) on the same snapshot reports
**20 vulnerabilities (7 moderate, 13 high)**, almost all under `@vercel/node`
and the eslint/typescript-eslint toolchain; only the `react-router` chain
belongs to the runtime dependencies. This total is the most date-sensitive
figure in this file.

Action: `npm audit fix` without `--force` removes the runtime finding. Do not
run `--force`: it proposes a two-major downgrade of `@vercel/node`.

## What holds up

- Secret hygiene is clean in the tree and the history.
- The engine is genuinely pure (no React, no I/O, no i18n), verified by import
  graph, which is why its fixture-based tests are fast and stable.
- `/api/coach` validates in a fixed order (method, origin, API key, body) with a
  hard cap on every caller-controlled field, never logs the body, and the tests
  pin all of it.
- Graceful degradation is real: without the API key or offline, training keeps
  working and the coach falls back to friendly copy.
- Video never leaves the browser; only aggregate metrics reach the backend.

## How to keep this file honest

A status here changes only in the PR that changes the code, with the PR
linked. New findings from later reviews go in a new dated file.
